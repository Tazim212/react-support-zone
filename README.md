1. What is JSX, and why is it used?
ans: JSX means Javascript XML.It is used for make reacts UI interactive.

2. What is the difference between State and Props?
ans: state is owned by the component itself and props owned by the parent component. 

3. What is the useState hook, and how does it work?
ans: useState hook is a hook which allows to add state in components.It takes 2 value in an array which is a value and a function. 

4. How can you share state between components in React?
ans: I can share state by props drilling between components in React.

5. How is event handling done in React?
ans: Event handling is done by using useState in React.